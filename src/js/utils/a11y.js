// const navButtons = document.querySelectorAll('.nav-button');
// const burgerMenuContainer = document.querySelector('.burger-menu-content');
// const headerNav = document.querySelector('.header-nav');

// const mq = window.matchMedia('(max-width: 600px)');

// function moveButtons(e) {
//     if (e.matches) {
//         navButtons.forEach(btn => burgerMenuContainer.appendChild(btn));
//     } else {
//         navButtons.forEach(btn => headerNav.appendChild(btn));
//     }
// }

// mq.addEventListener('change', moveButtons);
